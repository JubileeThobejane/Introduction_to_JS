/* Will an error occur if you try to access an array element with 
an index that is greater than or equal to the length of the array?

There will not be an error but it will return undefined

*/

let arr = [ 1 , 25 , 18 , 6 ];

console.log(arr.length);

console.log( arr[3]);