/*let counter = 0;

while (counter = 1) {
  console.log(counter);
  counter += 1;

  if (counter > 2) {
    break;
  }
}*/

//The above code is an infinite loop because the condition in the while statement
//uses an assignment instead of a comparison operator.